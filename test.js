const expect = chai.expect;
const assert = chai.assert;

describe('Week 6 Assignment:', () => {
    describe('Shuffle() function', () => {
        it('Should shuffle cards in the deck', () => {
            const deck = new Deck();
            deck.initializeDeck();
        })
    })
})